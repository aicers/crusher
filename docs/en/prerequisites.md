# Prerequisites

## Requirements

- `--cert`: Crusher certificate (PEM)
- `--key`: Crusher private key (PEM)
- `--ca-certs`: CA certificates for verifying the target server (PEM)
- Manager server address

## Additional Requirements for Local Configuration Mode

When the `-c` option is used, the following is additionally required:

- TOML configuration file
